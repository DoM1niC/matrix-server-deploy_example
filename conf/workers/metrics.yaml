worker_app: synapse.app.generic_worker
worker_name: metric_reader

worker_listeners:
    - type: http
      bind_address: '127.0.0.1'
      port: 9000
      resources:
        - names: [metrics, replication]

#worker_log_config: /etc/matrix-synapse/federation-reader-log.yaml