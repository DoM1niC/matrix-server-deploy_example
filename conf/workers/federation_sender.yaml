worker_app: synapse.app.generic_worker
worker_name: federation_sender

worker_listeners:
    - type: http
      bind_address: '127.0.0.1'
      port: 7007
      resources:
        - names: [metrics, replication]

worker_daemonize: false